#include <bits/stdc++.h>
#include "solution.h"

int main() {
    // This main is intentionally minimal; OJ may include/run its own tests.
    // To avoid unused template, instantiate with a trivial type and bound.
    SpeedCircularLinkedList<int, 97> tmp({97});
    // If run locally without tests, do nothing.
    return 0;
}

